//stored in /config.js
const BASE_URL = window.location.hostname === 'localhost' ? '' : '/coherency';

const config = {
  SITE_NAME: 'Coherency',
  COMPANY_NAME: 'Coherency',
  BASE_URL: BASE_URL,
};

export default config;
